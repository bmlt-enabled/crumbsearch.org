# SPA routing (index.html / virtual.html shells and the /virtual/* deep links) is
# handled in functions/_middleware.js, NOT here. On Cloudflare Pages, _redirects rules
# run BEFORE Functions, so a `/*` or `/virtual/*` rewrite here would short-circuit the
# middleware and break the per-meeting link-unfurl injection. Leave this file empty
# unless you need a redirect that must run ahead of the Function.
